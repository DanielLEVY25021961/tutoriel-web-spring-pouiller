<!DOCTYPE web-app PUBLIC
 "-//Sun Microsystems, Inc.//DTD Web Application 2.3//EN"
 "http://java.sun.com/dtd/web-app_2_3.dtd" >

<web-app>

	<!--  -->
	<display-name>tutoriel-web-spring-pouiller</display-name>

	
	<!-- ==================================================== -->
	<!--                      SPRING                          -->
	<!-- ==================================================== -->
	<!-- variable de contexte contextConfigLocation pointant 
	sur /WEB-INF/dispatcher-servlet.xml -->
	<context-param>
        <param-name>contextConfigLocation</param-name>
        <param-value>/WEB-INF/dispatcher-servlet.xml</param-value>
    </context-param>


	<!-- Le listener ContextLoaderListener charge la configuration Spring
	 à partir de la variable de contexte contextConfigLocation -->
    <listener>
        <listener-class>org.springframework.web.context.ContextLoaderListener</listener-class>
    </listener>

    
    <!-- Declaration de la servlet DispatcherServlet de Spring et de son mapping -->
    <!-- DispatcherServlet redirige les requêtes HTTP vers les contrôleurs voulus -->
    <!-- DispatcherServlet est équivalente à la « ActionServlet » de Struts). --> 
    <servlet>
        <servlet-name>servlet-dispatcher</servlet-name>
        <servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
        <init-param>
            <param-name>contextConfigLocation</param-name>
            <param-value>/WEB-INF/dispatcher-servlet.xml</param-value>
        </init-param>
        <load-on-startup>1</load-on-startup>
    </servlet>
    <servlet-mapping>
        <servlet-name>servlet-dispatcher</servlet-name>
        <url-pattern>/</url-pattern>
    </servlet-mapping>
 


    <!-- =============== -->
    <!-- PAGES D'ACCUEIL -->
    <!-- =============== -->
    <welcome-file-list>
		<welcome-file>vues/bonjour.jsp</welcome-file>
	</welcome-file-list>
	

	<!-- ==================================================== -->
	<!--                   BASES DE DONNEES                   -->
	<!-- ==================================================== -->
	
    <!-- Declaration de l'utilisation de la ressource JDBC -->
    <resource-ref>
        <description>Ressource JDBC de l'application</description>
        <res-ref-name>jdbc/dsAppliTutoWebSpringPouiller</res-ref-name>
        <res-type>javax.sql.DataSource</res-type>
        <res-auth>Container</res-auth>
        <res-sharing-scope>Shareable</res-sharing-scope>
    </resource-ref> 	
    
</web-app>
